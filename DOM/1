버퍼 수만큼 읽어서 만들기
버퍼로 넘어올수 있어서

fread 포인터를 읽어들이고 포인터로 왔다갔따
하면서 파싱해야한다.

현재 읽은거 포인터
포인터를 많이 써야한다.
현재 인덱스를 하나씩 증가하면서
읽어가면서 꺽쇠나오면 포인터로 받아서
엘리먼트 안에 담아두기

포인터가 뒤에가도 앞으로 갈수가 있다.
글자를 스캔하는 포인터가 필요
현재 인덱스에서 어트리뷰트가 필요할 때 다른 포인터를
증가시키면 태그네임 하나 추출가능.

돔 스트럭쳐 정의할 때 엘리먼트 값을 넣어주면된다.
메모리 버퍼

아이를 갖는게 있고 안갖는게 있을때 주의할것.
주석 가려내고. 
돔 노드는 트리를 구성하는 나뭇잎같은 역할
포인터가 부모포인터가 있고 자기가 이웃하는 포인터가 있고
자기 자식 포인터가 있다. 돔노드가 포인터를 가지고있다.
캐릭터 버퍼로 만들어서 인덱스로 얼록 해서 넣어라ㅓ
돔트리는 얼록덩어리를 여러개 만든거다
꺽새로 만든거를 따로 얼록시키는거다

나오는 내용 전부를 포인터로 받아서 얼록케이션으로 엘리먼트 안에다 담아두고
